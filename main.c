#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include"header_binary.h"
int main()
{int m;
int *n;
employee t[100];

char ch[100];
printf("give m\n");
scanf("%d",&m);


number_children(number_children);
search_first_name(number_children,ch);
incorporate(number_children,m);
delete_0(number_children);
save(number_children,t,&n);
sortt(number_children,t,&n);
return 0;
}
